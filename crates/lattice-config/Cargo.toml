[package]
name = "lattice-config"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
lattice-protocol = { path = "../lattice-protocol" }
lattice-core = { path = "../lattice-core" }
lattice-grammar = { path = "../lattice-grammar" }
lattice-completion = { path = "../lattice-completion" }
lattice-config-macros = { path = "../lattice-config-macros" }
arc-swap = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }
dirs = { workspace = true }
linkme = { workspace = true }
smallvec = { workspace = true }
# msg-mode.2: `messages.filter` validator parses the value via
# `tracing_subscriber::EnvFilter::try_new`. Same EnvFilter
# syntax the runtime's reload-handle accepts -- one parse
# rule, both at set-time validation and at runtime reload.
tracing-subscriber = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "options"
harness = false

[lints]
workspace = true
