# help-guest

The plugin's front page.

Registered under the **bare plugin id**, not `help-guest.help-guest` —
a one-page plugin should answer to `:help help-guest`, the way
`:help fugitive` does in vim.
