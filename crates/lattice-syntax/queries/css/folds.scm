; Tree-sitter fold ranges for CSS.
; Adapted from the Helix editor's runtime/queries/css/folds.scm
; (Mozilla Public License 2.0).

[
  (rule_set)
  (media_statement)
  (keyframes_statement)
  (at_rule)
  (supports_statement)
] @fold
