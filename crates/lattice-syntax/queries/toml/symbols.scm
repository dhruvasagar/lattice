; TOML: definition-position identifiers (table names, keys) for the
; `gen:tree-sitter-symbol` insert-completion source.
; (tree-sitter-toml-ng)

(table
  (dotted_key) @symbol)
(table_array_element
  (dotted_key) @symbol)
(pair
  .
  (_) @symbol)
