; Tree-sitter fold ranges for YAML.
; Adapted from the Helix editor's runtime/queries/yaml/folds.scm
; (Mozilla Public License 2.0).

[
  (block_mapping_pair)
  (block_sequence)
  (block_node)
  (document)
] @fold
