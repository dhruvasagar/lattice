* Features
- [ ] diagnostics under cursor
- [X] Terminal
- [X] ligatures
- [ ] variable size fonts in GUI, first consumer markdown
- [X] diffing
- [ ] Narrow mode
- [ ] digraphs & emoji's with picker
- [ ] Theme System (also with a theme picker with preview)
- [ ] Quickfix / Location lists
- [ ] :! shell execution
- [ ] ! filter operator
- [ ] :.! shell execution and replace range
- [ ] enhanced mini-buffer
- [ ] enhanced, configurable mode-line
- [ ] language support
- [ ] transient like menu
- [ ] compilation-mode
- [ ] surround operator
- [ ] argument operator
- [ ] Search operators
- [ ] borderless gui windows
- [ ] DAP
- [X] Picker to add <C-s>, <C-v>, <C-t> to open file under cursor in a
  split, vertical split or tab. This will override
- [X] {count}G, :{count} should jump to that line (preserve <C-o>)
- [ ] Create an api similar to `:jump` from vim that allows easy
  handling of any future jumps to automatically record jump history
  without requiring manual work arounds
- [ ] <C-g>, (:abort-all !?) to abort any running tasks
- [X] M-x as a command picker
  - [X] It should resolve to `:<command> |` for the user to enter
    arguments if the command requires arguments or just execute them
    if they don't take arguments
- [ ] focus mode
- [ ] history (command, macros, yank, etc.)
- [ ] auto indent
- [ ] auto formatting
- [ ] GPUI: Zooming Text
- [ ] Spell Check

* Plugins
- [ ] plugin manager
- [ ] project.el
- [ ] Claude Code IDE
- [ ] Org Mode
- [ ] lsp context
- [ ] which key
- [ ] Dictionary lookup

* Issues
- [X] GUI: cursor scrolls past viewport
- [X] command line completion
  - [X] file completion for `:e` is broken
- [X] `describe-*` functions open a popup, but the focus is not within them
- [X] <C-n>, <C-p> for completions
- [X] popup focus
- [X] `gg`, `zz`, `zt`, `zb` don't work
- [X] Mouse click on window does not work, no focus, can't maximize
- [X] Picker UI improve colors for matching, marginalia
- [X] LSP `K` (hover): popup doesn't appear on first press; requires
  an additional key press, and is not positioned at the cursor
  location. Two GPUI fixes (2026-05-27):
  - Wake-bridge in ~EditorView::new~ now drains ~run_tick_pending~
    before ~cx.notify()~ so idle LSP arrivals (hover, signature
    help, …) materialize without the next keystroke.
  - ~Editor::pending_hover_anchor~ stashes ~(cursor, scroll)~ at
    K-press; ~open_floating_popup~ consumes it one-shot so the
    popup pins to the invocation site rather than wherever the
    cursor drifted to between K and the LSP response.
  - Popup body now renders every row through a uniform
    ~flex_row~ of per-char divs (previously non-cursor lines
    used naked text, cursor line used per-char flex — metrics
    diverged, current line jumped a char right on every motion).
- [X] Popup buffers don't scroll when the cursor moves past the
  viewport (GPUI only — TUI was fine). Per-frame override on the
  GPUI render pass sets ~Editor::viewport_height~ to the popup's
  inner row count (~POPUP_INNER_HEIGHT_ROWS = 30~) while focused
  so ~ensure_cursor_in_viewport~ scrolls correctly; restores from
  the active leaf when the popup is dismissed.
- [X] `describe-buffer`'s "Active modes" entries are no longer
  rendered as links (regression — visual styling restored via
  ~lattice_help::with_markdown_syntax~ overlay; both peers now
  see Style::Link spans in popup.help_highlights).
- [X] Help-link follow (~<CR>~ on a link) works on TUI only.
  ~do_help_follow_link~ hoisted into ~Editor::do_help_follow_link~
  (2026-05-27); ~Action::FollowLink => BufferKind::Help~ routes to
  it. Both peers now dispatch through one host-side handler.
- [X] :terminal: in Normal mode, ~k~ doesn't move up. Root cause
  (2026-05-27): ~terminal-mode~ was registered in
  ~editor_boot.rs~ but never activated for terminal buffers, so
  ~resolve_invocation_runner~ returned None and motions fell
  through to ~run_document_invocation~ (the wrong buffer).
  Fixed by calling ~activate_major_for_buffer_kind(id,
  BufferKind::Terminal)~ in ~activate_buffer~'s Terminal arm.
- [X] Universal: cursor must never paint past the last visible
  buffer row. Resolved by the popup / terminal row-height locks
  (~popup_inner_height_rows~ derives from explicit chrome math,
  ~build_terminal_inner~ locks each row to ~estimated_row_px~,
  ~paint_pane~ uses ~pane.viewport_height~ for inactive paints).
- [X] :terminal: only half the vertical space appears used to
  render content (reported 2026-05-27 after row-lock fix). Needs
  interactive repro with ~RUST_LOG=lattice_gpui::viewport=debug~
  to confirm whether ~snap.rows~ vs ~leaf.viewport_height~ vs
  rendered pane height drift.
- [X] :b switches buffer in split → new buffer renders without
  syntax highlighting. Same root cause as terminal ~k~:
  ~resolve_invocation_runner~ was returning None for buffers
  whose major mode wasn't activated, so ~:b~ to a terminal
  buffer fell through to the document path and looked inactive.
  Resolved by the ~activate_major_for_buffer_kind~ call landed
  for terminal-mode (2026-05-27).
- [X] Popup body wrap: long lines wrap at the locked popup width
  (~popup.wrap~ option, default true). One source line emits one
  or more visible rows; cursor block follows the wrap segment
  whose char-range contains ~cursor_byte~.
- [X] Terminal inactive panes should also DIM the content (not
  just hide the cursor). Document panes today don't dim either —
  may need a peer-level opacity / palette-darken pass.
- [X] visual block mode does not work
- [X] marginalia columns alignment
- [ ] `scrolloff` not respected properly
- [X] ]e,[e, ]E, [E for multibuffer view not working
- [X] Any edit breaks syntax highlighting
- [X] `:e!` clones the buffer instead of just refreshing
- [X] `:e!` should not reposition cursor
- [X] *messages* buffer shows [+] in the :b list, it should not track
  dirty flag
- [X] hover popup position is off when there are splits
- [X] typing on rust, the inlay hint flicker is fixed but I notice as
  I type the comment fonts switch to italics sometimes, possibly when
  there are diagnostic errors on the line I am editing. They disappear
  when the line is syntactically valid.
- [X] When using `K`, and we focus on the popup, the decorations such
  as inlay-hint are removed from the background buffer (no reason to
  remove them), and when we close the popup and the background buffer
  gains focus again, they don't come back until the next key press
- [ ] Typing in rust buffer, the syntax highlighting lags behind by
  one character. The character just before the cursor doesn't get
  highlighted until one more key press.

* Documentation
- [ ] lattice-tutor

* Pending Slices
- [X] D.3–D.5/D.8 — diff system (inline overlay, side-by-side, hunk transfer, diffthis grouping)
- [X] M.2 — excerpt rendering in multibuffer
- [X] K.4 — multibuffer-is-a-regular-buffer audit
- [X] M.7/M.8 — excerpt and file-boundary fold providers
- [ ] LSP phase 4 — still in progress (4.5 wire complete, some trigger-UX rows deferred)
- [ ] Completion — :complete picker bridge
- [ ] Multibuffer View Providers
