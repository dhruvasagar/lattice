package lattice:plugin-host@0.1.0;

/// Mirrors `Candidate{Generator,Matcher,Ranker,Annotator}`
/// (lattice-completion). Guest exports; host wraps as `Arc<dyn>` via the
/// `insert_*` seam; reuses the `CandidateData::Extension` payload hatch for
/// plugin candidate data. Populated in PH7.6.
interface completion-source {}
